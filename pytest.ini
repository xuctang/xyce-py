[pytest]
testpaths = tests
markers =
    unit: fast unit tests that do not require Xyce
    property: hypothesis-backed property tests
    packaging: built artifact and install smoke tests
    xyce: real Xyce integration tests
